# Human review — sos-note-zero-is-an-answer

Generated by `npm run textos -- write`. Nothing below is a review: it is the packet a human reviews.
Publication on the site was mandated by the ShortsOS owner; this per-article review is still PENDING.

- Flow: `commit_to_content` → manifest surface `developer_note`
- Channel the evidence was checked for: `public_web`
- Buyer question: Will an AI video pipeline invent a story when my footage does not really support one?
- Target query: can ai make short videos from existing footage without inventing a story (HUMAN_CMO; demand NOT_MEASURED)
- TruthCheck: **pass** (0 finding(s))
- Writer: `geo-writer@0.2::operator:claude-code|geo-writer-slot@0.3` — TextOS `a0f6591057e01b97eda689bb83eddbf824007f0d`

## Slots, statements and the evidence each one cites

### title (title)

> Why ShortsOS made zero a valid answer when searching footage for stories

- **factual** — Why ShortsOS made zero a valid answer when searching footage for stories
  - `ev-n3-01` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «stops rejecting generations that produce 1 or 2 evidence-proven opportunities; caps the evidence-proven draft set at 3 after the evidence gate, never before; replaces the drafts.length===0 "rejected" branch with a distinct no_strong_story_available outcome, kept separate from provider failure ("failed")»

### meta (meta_description)

> In September 2026, ShortsOS let its story search report no strong story, stopped reading file dates as filming dates and fixed a cross-client lookup.

- **factual** — In September 2026, ShortsOS let its story search report no strong story, stopped reading file dates as filming dates and fixed a cross-client lookup.
  - `ev-n3-01` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «stops rejecting generations that produce 1 or 2 evidence-proven opportunities; caps the evidence-proven draft set at 3 after the evidence gate, never before; replaces the drafts.length===0 "rejected" branch with a distinct no_strong_story_available outcome, kept separate from provider failure ("failed")»
  - `ev-n3-04` (ShortsOS product commit 31b705b: fix(capture-time-truth): stop conflating Drive modifiedTime with capturedAt (FDTS-CAPTURE-TIME-TRUTH-V1)): «assets.capturedAt was written straight from Google Drive's modifiedTime with no provenance tag distinguishing it from a measured capture time -- modifiedTime changes on copy/re-share/re-upload, unrelated to when footage was actually shot.»
  - `ev-n3-06` (ShortsOS product commit 3803d15: fix(media-probe): tenant-scope and deterministic exact-duplicate lookup (FDTS-RETRIEVAL-SAFETY-V1)): «findDuplicateAssetId() matched fileHash across the entire media_probe_results table with no organization/client filter and no ORDER BY before .limit(1) — a P0 cross-tenant data leak (one client's asset could be reported as a duplicate of another client's asset on a hash collision) plus non-deterministic tie resolution.»

### lead (lead)

> Anyone who has handed a pile of raw clips to a tool knows the risk: the tool finds something, whether or not it is really there. ShortsOS changed its own story search so that finding no strong story became an honest result rather than an error. Its register lists this work as internal engineering with no independent public claim, so read it as history, not as a feature list.

- **editorial** — Anyone who has handed a pile of raw clips to a tool knows the risk: the tool finds something, whether or not it is really there.
- **factual** — ShortsOS changed its own story search so that finding no strong story became an honest result rather than an error.
  - `ev-n3-01` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «stops rejecting generations that produce 1 or 2 evidence-proven opportunities; caps the evidence-proven draft set at 3 after the evidence gate, never before; replaces the drafts.length===0 "rejected" branch with a distinct no_strong_story_available outcome, kept separate from provider failure ("failed")»
  - `ev-n3-03` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «Reconciles the ratified 0-3 strong-opportunity cardinality rule (FROM-DRIVE-TO-STORY-MANIFEST.md section 3.A) against the 3-7 implementation.»
- **factual** — Its register lists this work as internal engineering with no independent public claim, so read it as history, not as a feature list.
  - `ev-n3-08` (ShortsOS Public Truth manifest, FDTS-NARRATIVE-TRUTH-V1.claimCeiling): «No independent public claim»
  - `ev-n3-08b` (ShortsOS Public Truth manifest, FDTS-CAPTURE-TIME-TRUTH-V1.claimCeiling): «No independent public claim»
  - `ev-n3-08c` (ShortsOS Public Truth manifest, FDTS-REPEATED-TAKE-INTELLIGENCE-V1.claimCeiling): «No independent public claim»

### answer (answer) — What changed, in short?

> The story search in ShortsOS was moved from expecting three to seven strong stories to a rule of zero to three, where zero is a distinct, honest outcome. Technical failures, unfinished analysis and runs paused for budget stay separate outcomes, so a failure is not reported as a weak story. Two related fixes followed the same logic: filming dates are no longer guessed from file dates, and duplicate checks no longer look across clients.

- **factual** — The story search in ShortsOS was moved from expecting three to seven strong stories to a rule of zero to three, where zero is a distinct, honest outcome.
  - `ev-n3-01` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «stops rejecting generations that produce 1 or 2 evidence-proven opportunities; caps the evidence-proven draft set at 3 after the evidence gate, never before; replaces the drafts.length===0 "rejected" branch with a distinct no_strong_story_available outcome, kept separate from provider failure ("failed")»
  - `ev-n3-03` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «Reconciles the ratified 0-3 strong-opportunity cardinality rule (FROM-DRIVE-TO-STORY-MANIFEST.md section 3.A) against the 3-7 implementation.»
- **factual** — Technical failures, unfinished analysis and runs paused for budget stay separate outcomes, so a failure is not reported as a weak story.
  - `ev-n3-02` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «provider failure, incomplete analysis, and budget pause remain separate operational outcomes, never relabelled as weak storytelling.»
- **factual** — Two related fixes followed the same logic: filming dates are no longer guessed from file dates, and duplicate checks no longer look across clients.
  - `ev-n3-04` (ShortsOS product commit 31b705b: fix(capture-time-truth): stop conflating Drive modifiedTime with capturedAt (FDTS-CAPTURE-TIME-TRUTH-V1)): «assets.capturedAt was written straight from Google Drive's modifiedTime with no provenance tag distinguishing it from a measured capture time -- modifiedTime changes on copy/re-share/re-upload, unrelated to when footage was actually shot.»
  - `ev-n3-05` (ShortsOS product commit 31b705b: fix(capture-time-truth): stop conflating Drive modifiedTime with capturedAt (FDTS-CAPTURE-TIME-TRUTH-V1)): «Adds a pure resolveCaptureTime precedence function (embedded > explicit_metadata > declared > unknown; implausible/future-dated timestamps treated as absent; disagreements over 24h recorded, never averaged or silently dropped)»
  - `ev-n3-06` (ShortsOS product commit 3803d15: fix(media-probe): tenant-scope and deterministic exact-duplicate lookup (FDTS-RETRIEVAL-SAFETY-V1)): «findDuplicateAssetId() matched fileHash across the entire media_probe_results table with no organization/client filter and no ORDER BY before .limit(1) — a P0 cross-tenant data leak (one client's asset could be reported as a duplicate of another client's asset on a hash collision) plus non-deterministic tie resolution.»
  - `ev-n3-11` (ShortsOS product commit 3803d15: fix(media-probe): tenant-scope and deterministic exact-duplicate lookup (FDTS-RETRIEVAL-SAFETY-V1)): «findDuplicateAssetId now takes a required { organizationId, clientId } scope, joins media_probe_results to assets, and filters to the owning tenant.»

### section-1 (section) — Why no story can be the right answer

> Before the change, a search that found only one or two stories backed by the footage was rejected, and a search that found none was filed under the same rejection. After it, one, two or three stories backed by evidence are accepted, capped at three only once the evidence check has run, and zero gets its own outcome: no strong story available. The same change keeps technical trouble apart from editorial judgement, so a failed or paused run is not presented as footage without a story. For a brand, that distinction decides whether a no is information about its footage or noise from a system.

- **factual** — Before the change, a search that found only one or two stories backed by the footage was rejected, and a search that found none was filed under the same rejection.
  - `ev-n3-01` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «stops rejecting generations that produce 1 or 2 evidence-proven opportunities; caps the evidence-proven draft set at 3 after the evidence gate, never before; replaces the drafts.length===0 "rejected" branch with a distinct no_strong_story_available outcome, kept separate from provider failure ("failed")»
- **factual** — After it, one, two or three stories backed by evidence are accepted, capped at three only once the evidence check has run, and zero gets its own outcome: no strong story available.
  - `ev-n3-01` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «stops rejecting generations that produce 1 or 2 evidence-proven opportunities; caps the evidence-proven draft set at 3 after the evidence gate, never before; replaces the drafts.length===0 "rejected" branch with a distinct no_strong_story_available outcome, kept separate from provider failure ("failed")»
- **factual** — The same change keeps technical trouble apart from editorial judgement, so a failed or paused run is not presented as footage without a story.
  - `ev-n3-02` (ShortsOS product commit 5f845dc: fix(content-inventory): reconcile opportunity cardinality from 3-7 to 0-3 (FDTS-NARRATIVE-TRUTH-V1)): «provider failure, incomplete analysis, and budget pause remain separate operational outcomes, never relabelled as weak storytelling.»
- **editorial** — For a brand, that distinction decides whether a no is information about its footage or noise from a system.

### section-2 (section) — Dates and duplicates, fixed for the same reason

> ShortsOS used to record a clip's filming time straight from the date the file was last modified in Google Drive, a date that changes whenever a file is copied, re-shared or re-uploaded. The fix reads the time embedded in the video first, then explicit metadata, then what the client declared, and otherwise marks the date as unknown, recording disagreements instead of averaging them. Where no reliable date exists, the product is built to show unknown rather than invent one. A separate check that spots duplicate clips compared files across all clients with no filter, so one client's clip could be reported as a duplicate of another client's. The corrected check only looks within the client that owns the clip. That fix was proven against an isolated test database with four regression cases, which is not proof of isolation under real load across many clients.

- **factual** — ShortsOS used to record a clip's filming time straight from the date the file was last modified in Google Drive, a date that changes whenever a file is copied, re-shared or re-uploaded.
  - `ev-n3-04` (ShortsOS product commit 31b705b: fix(capture-time-truth): stop conflating Drive modifiedTime with capturedAt (FDTS-CAPTURE-TIME-TRUTH-V1)): «assets.capturedAt was written straight from Google Drive's modifiedTime with no provenance tag distinguishing it from a measured capture time -- modifiedTime changes on copy/re-share/re-upload, unrelated to when footage was actually shot.»
- **factual** — The fix reads the time embedded in the video first, then explicit metadata, then what the client declared, and otherwise marks the date as unknown, recording disagreements instead of averaging them.
  - `ev-n3-05` (ShortsOS product commit 31b705b: fix(capture-time-truth): stop conflating Drive modifiedTime with capturedAt (FDTS-CAPTURE-TIME-TRUTH-V1)): «Adds a pure resolveCaptureTime precedence function (embedded > explicit_metadata > declared > unknown; implausible/future-dated timestamps treated as absent; disagreements over 24h recorded, never averaged or silently dropped)»
- **factual** — Where no reliable date exists, the product is built to show unknown rather than invent one.
  - `ev-n3-09` (ShortsOS Public Truth manifest, FDTS-CAPTURE-TIME-TRUTH-V1.prohibitedClaims): «UNKNOWN must render as UNKNOWN by design.»
- **factual** — A separate check that spots duplicate clips compared files across all clients with no filter, so one client's clip could be reported as a duplicate of another client's.
  - `ev-n3-06` (ShortsOS product commit 3803d15: fix(media-probe): tenant-scope and deterministic exact-duplicate lookup (FDTS-RETRIEVAL-SAFETY-V1)): «findDuplicateAssetId() matched fileHash across the entire media_probe_results table with no organization/client filter and no ORDER BY before .limit(1) — a P0 cross-tenant data leak (one client's asset could be reported as a duplicate of another client's asset on a hash collision) plus non-deterministic tie resolution.»
- **factual** — The corrected check only looks within the client that owns the clip.
  - `ev-n3-11` (ShortsOS product commit 3803d15: fix(media-probe): tenant-scope and deterministic exact-duplicate lookup (FDTS-RETRIEVAL-SAFETY-V1)): «findDuplicateAssetId now takes a required { organizationId, clientId } scope, joins media_probe_results to assets, and filters to the owning tenant.»
- **factual** — That fix was proven against an isolated test database with four regression cases, which is not proof of isolation under real load across many clients.
  - `ev-n3-12` (ShortsOS Public Truth manifest, FDTS-RETRIEVAL-SAFETY-V1.claimCeiling): «proven against an isolated Neon TEST branch with four regression cases»
  - `ev-n3-10` (ShortsOS Public Truth manifest, FDTS-RETRIEVAL-SAFETY-V1.prohibitedClaims): «Any claim this proves production-scale tenant isolation under real multi-tenant load.»

### section-3 (section) — Similar takes are grouped, not thrown away

> When a shoot produces several takes of the same moment, ShortsOS groups them rather than excluding any clip merely because it looks similar to another. All clips still appear in the results, and supporting evidence is counted by distinct group, so three takes of one moment do not pass for three separate proofs. The grouping relies on signals the system already recorded, uses no AI similarity model, and does not judge what two clips mean. Taken together, these fixes make the pipeline readier to say what it does not know.

- **factual** — When a shoot produces several takes of the same moment, ShortsOS groups them rather than excluding any clip merely because it looks similar to another.
  - `ev-n3-07` (ShortsOS product commit 5509dfc: feat(content-inventory): add deterministic repeated-take/near-duplicate signal (FDTS-REPEATED-TAKE-INTELLIGENCE-V1)): «Doctrine: never hard-exclude a candidate merely on weak semantic similarity -- every input candidate appears in the output, grouped via a deterministic groupId, so a downstream evidence-counter can count distinct groups instead of distinct segments.»
- **factual** — All clips still appear in the results, and supporting evidence is counted by distinct group, so three takes of one moment do not pass for three separate proofs.
  - `ev-n3-07` (ShortsOS product commit 5509dfc: feat(content-inventory): add deterministic repeated-take/near-duplicate signal (FDTS-REPEATED-TAKE-INTELLIGENCE-V1)): «Doctrine: never hard-exclude a candidate merely on weak semantic similarity -- every input candidate appears in the output, grouped via a deterministic groupId, so a downstream evidence-counter can count distinct groups instead of distinct segments.»
- **factual** — The grouping relies on signals the system already recorded, uses no AI similarity model, and does not judge what two clips mean.
  - `ev-n3-13` (ShortsOS Public Truth manifest, FDTS-REPEATED-TAKE-INTELLIGENCE-V1.claimCeiling): «deterministic repeated-take/near-duplicate classification, retrieval-level only, no embeddings»
- **editorial** — Taken together, these fixes make the pipeline readier to say what it does not know.

## Prohibited claims to check by hand

TextOS does not scan for these (a substring scan would block honest denials and miss paraphrases); the reviewer does.

**FDTS-NARRATIVE-TRUTH-V1**

- Any user-visible strength/truth/series copy — explicitly out of this capability's own scope.

**FDTS-CAPTURE-TIME-TRUTH-V1**

- Any claim of a resolved capture time where evidence is genuinely absent — UNKNOWN must render as UNKNOWN by design.

**FDTS-RETRIEVAL-SAFETY-V1**

- Any claim this proves production-scale tenant isolation under real multi-tenant load.

**FDTS-REPEATED-TAKE-INTELLIGENCE-V1**

- Any semantic-identity claim beyond available evidence — explicitly out of scope by this capability's own doctrine constraint.

## Reviewer checklist

- [ ] Every factual sentence says no more than the evidence quoted under it.
- [ ] No sentence implies self-serve access, general availability, repeated volume, or guaranteed quality or performance.
- [ ] Historical statements are dated or clearly past; nothing reads as a current offer unless the manifest authorizes it on this surface.
- [ ] Reviewer name and date recorded in the brief's review record.

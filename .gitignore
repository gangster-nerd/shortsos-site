node_modules/
.next/
out/
*.tsbuildinfo
next-env.d.ts
.DS_Store
.env*.local
.vercel
.env*

# TextOS slot prompts: regenerated by `npm run textos -- prepare`; only their hashes are
# committed (runs/<id>/adapt.json). The prompt text is TextOS's, not this public repo's.
textos/runs/*/prompts/
